Host *
    # Prevent SSH connections timing out. This will poll the server every 60"
    ServerAliveInterval 60
    ServerAliveCountMax 3

    # Use my local SSH key to authenticate on the remote hosts (when tunelling)
    ForwardAgent yes
